{
  "buildCommand": "cd placement-app && npm install && npm run build",
  "outputDirectory": "placement-app/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
